{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon src/server-express.ts",
    "build:deno": "deno compile --output ./dist/server-deno ./src/server-deno.ts",
    "build:express": "tsc",
    "start:deno": "deno run --allow-net --allow-read --config deno.json dist/server-deno.js",
    "start:express": "node dist/server-express.js"
  },
  "type": "module",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.0",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
